# PC-SAFT data

Vendored verbatim from upstream CoolProp v8.0.0 (`dev/pcsaft/`), tag
`gui-v8.0.0`. Input to `rustprop-datagen`'s PC-SAFT emitter — never hand
edited.

- `all_pcsaft_fluids.json` — 180 fluids (18 associating, 66 polar, 26 ionic)
- `mixture_binary_pairs_pcsaft.json` — 140 kij records (2 carry kijT)
